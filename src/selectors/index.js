import * as taskModalSelectors from './taskModal'
import * as tasksSelectors from './tasks'
import * as confirmationModalSelectors from './confirmationModal'

export {
    taskModalSelectors,
    tasksSelectors,
    confirmationModalSelectors,
}